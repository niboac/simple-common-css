{
  "name": "simple-common-css",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "css": "postcss src/*.css --dir css",
    "serve": "http-server -p 8086",
    "v": "npm version patch",
    "test": "echo \"Error: no test specified\" && exit 1",
    "pub": "npm run css && npm config set registry https://registry.npmjs.org && npm publish && npm config set registry https://registry.npmmirror.com"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "normalize.css": "^8.0.1"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.17",
    "postcss": "^8.4.33",
    "postcss-cli": "^11.0.0",
    "vite": "^5.0.11"
  }
}